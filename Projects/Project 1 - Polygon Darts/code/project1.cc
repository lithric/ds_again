#include "fraction.h"
#include "point.h"

bool intersect(Point p1, Point p2, Point q1, Point q2) {
    Point p_length_vec = p2 - p1;
    Point q_length_vec = q2 - q1;

    Point point_diff_vec = q1 - p1;
}

int main() {
    return 0;
}